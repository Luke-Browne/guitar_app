import 'package:flutter/material.dart';

class Fretboard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
      child: Center(child: Text("Fretboard")),
    );
  }
}